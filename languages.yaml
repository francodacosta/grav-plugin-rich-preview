en:
  PLUGIN_RICH_PREVIEW:
    DESC_LABEL: Page description
    DESC_HELP: can be left bank, if so it will use the metadata description field of try to get it from the page content
    THUMB_LABEL: Page thumbnail
    THUMB_HELP: image url or page media image order number, if left bank it will use first image in page media
